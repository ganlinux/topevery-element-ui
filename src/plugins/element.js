import Vue from 'vue'
import { Button, Dialog, Select, Input, Table, TableColumn } from 'element-ui'

// Vue.component(Button.name, Button)
// Vue.component(Select.name, Select)

Vue.use(Input)
Vue.use(Table)
Vue.use(TableColumn)
Vue.use(Button)
Vue.use(Select)
Vue.use(Dialog)
